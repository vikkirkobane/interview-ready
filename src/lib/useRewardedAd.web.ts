/**
 * Web stub for useRewardedAd without importing native react-native-google-mobile-ads.
 */
export function useRewardedAd(_options?: { onRewarded?: (amount: number, type: string) => void }) {
  return {
    showAd: () => {},
    loaded: false,
  };
}
